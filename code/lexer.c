#include "constructs.h"

flag shouldRun = True;

dt_dfaState s = 1;
char input;

// read input file into buffer
char * buffer;

// define indices
int begin, forward;

char getNextChar(buffer, )

while(shouldRun)
{
	switch(s)
	{
		case 1:

			if(input=='[')
			{
				s = 4
			}

			break;
	}
}